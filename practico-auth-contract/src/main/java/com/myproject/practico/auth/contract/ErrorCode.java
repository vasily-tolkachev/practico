package com.myproject.practico.auth.contract;

public enum ErrorCode {
    INVALID_REQUEST,
    UNAUTHORIZED,
    FORBIDDEN,
    TOKEN_EXPIRED,
    TOKEN_INVALID,
    PROVIDER_UNAVAILABLE,
    INTERNAL_ERROR
}
