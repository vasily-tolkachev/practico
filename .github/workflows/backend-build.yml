name: Backend Build And Publish

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  CORE_IMAGE_NAME: ${{ github.repository_owner }}/mastery-core
  AUTH_IMAGE_NAME: ${{ github.repository_owner }}/mastery-auth

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
          cache: maven

      - name: Build jars (skip tests)
        run: mvn -q -Dmaven.test.skip=true package

  docker-publish:
    runs-on: ubuntu-latest
    needs: build-test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
          cache: maven

      - name: Build jars (skip tests)
        run: mvn -q -Dmaven.test.skip=true package

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push auth image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: practico-auth-service/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.AUTH_IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.AUTH_IMAGE_NAME }}:latest

      - name: Build and push core image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: practico-core-service/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.CORE_IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ env.CORE_IMAGE_NAME }}:latest

      - name: Trigger infrastructure deploy
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.INFRA_REPO_DISPATCH_TOKEN }}
          repository: ${{ github.repository_owner }}/mastery-infrastructure
          event-type: core-auth-images-published
          client-payload: >-
            {"source_repo":"${{ github.repository }}","source_sha":"${{ github.sha }}","core_image":"${{ env.REGISTRY }}/${{ env.CORE_IMAGE_NAME }}:${{ github.sha }}","auth_image":"${{ env.REGISTRY }}/${{ env.AUTH_IMAGE_NAME }}:${{ github.sha }}"}
